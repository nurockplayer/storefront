version: "3"

services:
  saleor-storefront:
    container_name: saleor-storefront
    build:
      dockerfile: Dockerfile
      args:
        NEXT_PUBLIC_SALEOR_API_URL: ${NEXT_PUBLIC_SALEOR_API_URL}
        NEXT_PUBLIC_STOREFRONT_URL: ${NEXT_PUBLIC_STOREFRONT_URL}
        NEXT_PUBLIC_TACHIYA_API_URL: ${NEXT_PUBLIC_TACHIYA_API_URL}
    restart: always
    environment:
      TACHIYA_API_URL: ${TACHIYA_API_URL}
      TACHIYA_INTERNAL_SHARED_SECRET: ${TACHIYA_INTERNAL_SHARED_SECRET}
    ports:
      - 3000:3000
    networks:
      - saleor_network

  # Add more containers below (nginx, postgres, etc.)

networks:
  saleor_network:
    external: false
